Typewriter media now uses `public/Typewritter-alpha.webm` as the primary asset, with `public/Typewritter.mp4` as a fallback.
The transparent WebM was keyed from the original white-background MP4 so the About page can render the typewriter without a visible box behind it.
